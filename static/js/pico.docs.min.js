!function () {
    if (window.matchMedia("(min-width: 992px)").matches) {
        var e = document.querySelector("aside nav"), t = document.querySelectorAll("aside details");
        if (e.clientHeight < e.scrollHeight) for (var r = 1; r < t.length; r++) t[r].removeAttribute("open")
    }
}(), function () {
    function a(e) {
        if ("light" == e) var t = n.button.on; else t = n.button.off;
        document.querySelector("html").setAttribute("data-theme", e);
        for (var r = document.querySelectorAll(n.selector), a = 0; a < r.length; a++) r[a].innerHTML = t, r[a].setAttribute("aria-label", t.replace(/<[^>]*>?/gm, ""));
        n.currentTheme = e
    }

    var n = {
        button: {
            element: "BUTTON",
            class: "contrast switcher theme-switcher",
            on: "<i>Turn on dark mode</i>",
            off: "<i>Turn off dark mode</i>"
        },
        target: "body",
        selector: "button.theme-switcher",
        currentTheme: window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light"
    };
    !function () {
        var e = document.createElement(n.button.element);
        e.className = n.button.class, document.querySelector(n.target).appendChild(e), a(n.currentTheme);
        for (var t = document.querySelectorAll(n.selector), r = 0; r < t.length; r++) t[r].addEventListener("click", function (e) {
            "light" == n.currentTheme ? a("dark") : a("light")
        }, !1)
    }()
}()